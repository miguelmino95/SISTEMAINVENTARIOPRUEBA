@model IEnumerable<SistemaInventarios.Models.materiales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodMaterial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodMaterial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMaterial }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMaterial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMaterial })
        </td>
    </tr>
}

</table>
